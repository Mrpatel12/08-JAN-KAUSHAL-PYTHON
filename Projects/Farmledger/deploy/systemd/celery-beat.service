[Unit]
Description=Celery Beat for Farmledger
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/app
Environment="PATH=/app/.venv/bin"
ExecStart=/app/.venv/bin/celery -A Farmledger beat --loglevel=info

[Install]
WantedBy=multi-user.target
